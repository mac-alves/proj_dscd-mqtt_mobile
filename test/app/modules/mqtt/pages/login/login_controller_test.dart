import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:mobile/app/modules/mqtt/pages/login/login_controller.dart';
import 'package:mobile/app/modules/mqtt/mqtt_module.dart';

void main() {
  initModule(MqttModule());
  // LoginController login;
  //
  setUp(() {
    //     login = MqttModule.to.get<LoginController>();
  });

  group('LoginController Test', () {
    //   test("First Test", () {
    //     expect(login, isInstanceOf<LoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(login.value, equals(0));
    //     login.increment();
    //     expect(login.value, equals(1));
    //   });
  });
}
